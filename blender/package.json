{
  "name": "blender",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ blender",
    "create-local": "graph create --node http://localhost:8020/ blender",
    "remove-local": "graph remove --node http://localhost:8020/ blender",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs https://ipfs.satsuma.xyz blender",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
